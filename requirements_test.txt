# Tooling for local checks. The helpers tests only need pytest; the optional
# full HA entity tests need the HA custom-component test harness (Python 3.13).
pytest>=8
warema-wms-controller==0.2.4
# pytest-homeassistant-custom-component  # uncomment for full HA integration tests
